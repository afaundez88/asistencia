
{
  "name": "asistencia-monorepo", 
  "version": "1.0.0",
  "private": true, 
  "scripts": {
   
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "echo \"No dev script for plain HTML frontend, just open index.html\"", 
    "install:all": "npm install --prefix backend && npm install --prefix frontend" 
  }
  
}