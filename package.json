// asistencia/package.json (RAÍZ DEL PROYECTO)
{
  "name": "asistencia-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "echo \"Frontend estático, abrir index.html directamente o usar un live server\"",
    "install:backend": "npm install --prefix backend",
    "install:frontend": "echo \"No hay dependencias de npm para el frontend estático\""
    // Puedes añadir más scripts aquí si los necesitas para coordinar
  }
  // Asegúrate de que NO haya secciones "main", "dependencies", o "devDependencies" aquí,
  // a menos que sean realmente para herramientas globales del monorepo (poco común para este caso).
}